name: vulnerax
services:
  - name: vulnerax
    type: web
    dockerfile: Dockerfile
    instance_type: free
    ports:
      - port: 8000
        protocol: http
    routes:
      - path: /
        port: 8000
    env:
      - key: PORT
        value: "8000"
